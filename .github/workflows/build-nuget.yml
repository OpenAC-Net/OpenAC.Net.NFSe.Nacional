# yaml-language-server: $schema=https://json.schemastore.org/github-workflow. json
#
# GitHub Actions Workflow para Build e Publicação de Pacotes NuGet
#
# Este workflow constrói o pacote NuGet personalizado CMSol. Fiscal.OpenAC. Net. NFSe.Nacional
# sem modificar os arquivos . csproj do projeto original.
#
# Triggers: 
# - Manual (workflow_dispatch): Build com sufixo customizável e opção de publicar no NuGet. org
# - Push to main: Build automático com sufixo 'fork' (sem publicação)
# - Tags v*: Build com versão da tag + GitHub Release + Publicação automática no NuGet.org
#
# Publicação no NuGet.org:
# - Manual: Apenas se checkbox "Publicar no NuGet.org?" estiver marcada
# - Tags v*: Publicação automática
# - Usa Trusted Publishing (OIDC) - sem API keys de longa duração
# - Requer configuração prévia no NuGet.org com: 
#   - Organization: CMSol.Fiscal
#   - Repository Owner: renatoguarilha
#   - Repository: OpenAC.Net.NFSe.Nacional
#   - Workflow:  build-nuget.yml
#
name: 📦 Build Custom NuGet Package

on:
  # 🎯 Manual execution with custom

